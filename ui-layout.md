# OI IDE UI 布局草案

## 布局总览
- 顶部：菜单栏 + 快捷操作条（运行/测试/对拍/格式化/切题按钮）。
- 左栏：活动栏（常驻：资源管理器、测试样例、数据/对拍、题解、源码片段、Git）。活动栏顶部保留 logo；底部不放按钮。
- 左侧主侧边栏：随 tab 显示对应视图；支持折叠/分屏。默认展开资源管理器与样例面板。
- 主区：多标签编辑器，支持拆分视图。右侧可选辅助栏（变量/调用栈/监视/性能）。
- 底部面板：终端、测试结果、对拍日志、问题列表、运行性能面板。支持自动弹出失败结果。
- 右下角状态区：编译标准(C++17/20/23)、编译模板(O2/调试)、当前题目/题号、时间/内存限。可一键切换。

## 顶部菜单与快捷区
- 菜单：文件/编辑/视图/运行/调试/数据/对拍/题解/帮助。默认展开菜单栏（非紧凑模式）。
- 快捷区（可定制）：运行当前代码、批量样例测试、一键对拍、格式化、清理调试输出、切换编译模板、打开题目链接、导入样例。

## 左侧活动栏与侧边栏分区
1) 资源管理器：最简文件树，突出当前题目文件；支持快速创建/切换 `*.cpp` 草稿。
2) 样例测试面板：
   - 样例列表：标签、排序、开关；批量导入/导出 .in/.out/zip；一键复制 OJ 样例自动拆分。
   - 预览/编辑：左右分栏（输入/期望输出），下方运行参数（时限/内存/对比规则）。
   - 结果标签：按样例展示通过/失败/超时，并显示耗时/内存；失败样例高亮差异。
3) 数据/对拍面板：
   - 数据生成：参数表单 + 生成器代码区；预设模板（图论/数组/字符串）。
   - 数据集列表：编号、大小、标签；批量重命名/删除/校验；可视化预览（图/数组统计）。
   - 对拍配置：选择 gen/sol/std，次数、并发线程、时限/内存、SPJ 选项；结果流式输出，首次差异立即停并保存输入/输出。
4) 题解面板：
   - Markdown + KaTeX 预览双栏；目录树自动生成；代码片段插入带行号；可导出 PDF/HTML/MD。
   - 代码引用联动：点击题解中的行号跳转代码；代码更新时提示同步。
5) 代码片段库：分层目录 + 标签过滤 + 搜索；一键插入到光标；支持导入导出。
6) Git：最小化视图（变更/提交/分支），适配训练场景。

## 主编辑区
- 多标签 + 拆分视图：典型布局“代码 | 样例编辑”或“代码 | 题解”。
- 内置编译模板切换：编辑器右上角下拉（C++17/20/23，O2/Debug）。
- 状态行：当前样例/数据集、测试模式、诊断计数、光标位置。

## 底部面板
- 终端：预设命令（编译、运行、生成数据、对拍启动）。
- 测试结果：表格按样例展示状态/耗时/内存/期望 vs 实际差异；失败行可“一键调试该样例”。
- 对拍日志：按轮次显示，用例输入片段、两份输出摘要、差异指示；提供“打开完整输入/输出”。
- 问题列表：编译/静态分析诊断；点击定位。
- 性能面板：火焰图/时间线（可延后实现）；至少显示总耗时和峰值内存。

## 调试与可视化
- 断点：条件/日志断点；调试入口允许选择样例或数据集。
- 监视/变量/调用栈：放右侧辅助栏，默认折叠；树/堆/图可视化入口从变量右键打开。
- 控制台：支持快速重放最近输入（绑定样例）。

## 竞赛/练习模式切换
- 练习模式：全部面板可见，显示样例/数据/对拍工具。
- 比赛模式：一键“比赛视图”，只保留代码+样例结果+终端，隐藏题解/Git，减少干扰；可定时器悬浮。

## 入口与全局操作
- 首屏欢迎页：快速打开/创建工作区，最近题目/代码，导入样例 zip，打开对拍模板。
- 命令面板：提供“导入 OJ 样例”“运行当前样例”“运行全部样例”“启动对拍”“生成数据集”“打开题解”。

## 主题与可定制
- 预设暗色为主；支持字号/行高/配色自定义；活动栏/侧边栏宽度可拖拽。
- 快捷键方案预设：VSCode/Vim/Emacs/OI 方案；可导入导出。

## 布局示例（文字示意）
- 顶部：菜单栏 + 快捷操作条
- 左侧：活动栏（垂直图标） → 侧边栏（资源管理 / 样例 / 数据+对拍 / 题解 / 片段 / Git）
- 中间：编辑器（可分屏），右侧可展开调试辅助栏
- 底部：终端 | 测试结果 | 对拍日志 | 问题 | 性能
- 状态行：编译标准/模板、题号、时限/内存、诊断计数、光标位置

# 单文件 C++ 特化体验（快速刷题/对拍）
目标：面向“单文件 cpp + OJ 样例”的极简模式，降低项目管理干扰，强化编译/运行/样例/对拍直达。

## 入口与识别
- 当工作区仅包含单个/少量 `.cpp`（无 CMake/多文件）时自动提示“单文件模式”。
- 顶部 banner 或状态行展示：`单文件模式 | C++20 | O2`，可一键切换标准/优化模板。
- 默认展开“样例测试”面板；资源管理器简化为文件列表（当前文件置顶）。

## 快捷工具条（单文件模式）
- 运行当前文件：`g++ -std=c++20 -O2`（可选 Debug 模板），运行后将输入焦点切到终端/输出。
- 一键样例：批量执行当前样例集，失败则弹出底部结果面板。
- 一键对拍：需要选择/记忆 gen/std/sol，循环次数、线程数、时限/内存可快速切换。
- 清理/重置：清空输出、终端历史、恢复默认编译参数。

## 样例/运行区优化
- 样例列表悬浮在左侧面板顶部，支持快捷键切换上一/下一样例；回车立即运行该样例。
- 输入/期望输出双栏，右侧实时 diff 实际输出；下方展示耗时/内存。
- 失败样例提供“用此输入启动调试”按钮，直跳 DAP，会自动填充 stdin。
- 样例导入：支持从剪贴板一键拆分多组；支持 zip 的 `.in/.out` 批量导入。

## 编译/运行配置预设
- 预设模板：`O2`、`Debug(-g -O0)`、`Sanitize(asan/ubsan)`、`Cf/Gcc OJ`（可映射到不同 g++ 版本）。
- 状态行快速切换标准：`C++17/20/23`，默认 C++20。
- 允许为单文件保存独立的编译模板选择和运行参数。

## 对拍面板（单文件快捷版）
- 选择路径：gen.cpp / std.cpp / sol.cpp，自动记忆最近选择；可从当前文件派生（复制为 std/sol）。
- 参数卡片：次数、线程、时限、内存、SPJ on/off；保存为预设。
- 结果：流式显示，对首个差异停止并保存 `last_fail.in/out/std.out`，提供“用此输入调试 sol”。

## 文件与片段
- 默认在侧边展示“常用模板”抽屉：FastIO、常用宏、DS/图模板，可一键插入到当前光标。
- 新建单文件对话：提供 OI 模板（含 `ios::sync_with_stdio(false); cin.tie(nullptr);`）。

## 布局收敛
- 左栏：资源管理简化 + 样例面板默认展开。
- 主区：代码 + 样例（垂直拆分）；底部：测试/对拍/终端自动切换。
- 右侧辅助栏默认折叠，仅在调试时展示变量/调用栈。

## OptiCode IDE 集成要点（实现提示）
- 通过配置 `defaultPreferences` 关闭 menubar 紧凑模式、默认展开样例视图。
- 借助 `main-layout` 贡献点，为单文件模式提供独立的 layout preset（加载时根据工作区结构切换）。
- 样例/对拍/生成/运行可基于 `terminal-next` + `process` 统一封装命令，结果送入专用 panel（extension view）。
- 利用贡献点机制在状态栏、快捷工具条注册单文件专用命令（切标准/模板、运行样例、对拍）。

# 比赛模式（多题单工作区切换）
目标：一场比赛 4~5 题，在同一工作区快速切题，隔离样例/编译配置，减少视图干扰。

## 题目工作单元
- 按比赛创建 `contest-X/`，题目子目录 `A/ B/ C/ ...`，每题默认 `main.cpp`、`samples/`、`data/`、`notes.md`。
- 左侧“题目列表”视图：按题号排序，显示状态（未测/通过/失败），快捷键上下切换；切题时自动加载该题的样例、编译模板、对拍配置。
- 每题维护独立：编译模板、样例集、对拍参数、运行参数，切换题目自动保存/恢复。

## 切题与上下文同步
- 切换题目时：关闭当前题临时终端/对拍任务；主编辑器跳转到该题 `main.cpp`；样例面板加载该题样例；底部测试面板显示该题最近结果；状态栏显示“Contest X | 题目 A | C++20 | O2 | 时限/内存”。

## 快捷操作（比赛视图）
- 顶部工具条：运行当前题、测试当前题样例、对拍当前题、提交前检查（移除调试输出/格式化）。
- 题目列表右键：复制题号、打开题目链接、导入官方样例 zip、标记通过/未通过。
- 比赛视图下隐藏题解/Git 等干扰面板，仅保留代码/样例/终端/测试结果；可一键切回练习模式。

## 样例与数据隔离
- 每题样例存于 `A/samples/*.in|out`，对拍数据存于 `A/data/`；导入/生成/失败样例均落在对应题目录。
- 运行/调试/对拍命令自动以题目目录为 cwd，避免交叉污染。

## 计时与提醒
- 顶部或状态栏显示比赛计时器，可设开始时间与总时长；支持暂停/继续；时间到提示但不强打断。

## 实现提示
- 利用 main-layout 贡献点注册“题目列表”侧栏视图，与资源管理器并列。
- 为题目上下文引入轻量 Store（题号→编译模板/样例/对拍配置/状态），切换时读写。
- 终端/进程命令注入 `cwd` 为题目录；样例/对拍 panel 读取题目相对路径。
