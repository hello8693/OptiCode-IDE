{
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch Electron Forge (yarn start)",
            "runtimeExecutable": "yarn",
            "runtimeArgs": [
                "start"
            ],
            "cwd": "${workspaceFolder}",
            "internalConsoleOptions": "neverOpen"
        }
    ]
}
